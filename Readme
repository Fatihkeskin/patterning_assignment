Suggestions for improvement

The last unit test method named "printNonEqualMatrix" summarizes it. Maybe the algorithm can be enhanced for solving the 2D arrays having different sizes of arrays.

Cache aware or cache oblivious algorithms can be implemented for better utilization of CPU Caches.

